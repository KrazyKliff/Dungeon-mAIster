export interface Location {
  id: string;
  name: string;
  description: string;
  // Add other properties as needed, for example:
  // coordinates: { x: number; y: number; };
  // type: 'city' | 'dungeon' | 'forest';
  // npcs: string[]; // List of NPC IDs
}
